let username = prompt(`Enter the username`);
let email = prompt(`Enter the emal`);
let password = prompt(`Enter the password`);

let user = {
    username,
    email,
    password
}

console.log(user);